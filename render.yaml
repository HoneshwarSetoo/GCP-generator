services:
  - type: web
    name: your-service
    runtime: python
    buildCommand: |
      apt-get update && apt-get install -y gdal-bin libgdal-dev python3-gdal
      pip install GDAL==$(gdal-config --version) -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
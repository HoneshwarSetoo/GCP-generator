fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
# GDAL cannot be installed via plain `pip install GDAL` on Windows (requires C++ Build Tools).
# For Python 3.10 on Windows, use the pre-built wheel from Gohlke's GitHub releases:
#
#   Step 1 — Download wheel to temp (avoids Windows file-lock issues):
#     python -c "import urllib.request, os; urllib.request.urlretrieve('https://github.com/cgohlke/geospatial-wheels/releases/download/v2025.1.20/GDAL-3.10.1-cp310-cp310-win_amd64.whl', os.path.join(os.environ['TEMP'], 'GDAL.whl'))"
#
#   Step 2 — Install from temp:
#     pip install "%TEMP%\GDAL.whl"
#
# For Python 3.11+ or other platforms, check: https://github.com/cgohlke/geospatial-wheels/releases
